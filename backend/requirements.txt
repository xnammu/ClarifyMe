# API layer
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# depixelization_poc deps
Pillow>=10.0.0

# DepixHMM deps (upstream pins numpy==1.21.5/Pillow==8.3.2/scikit-learn==0.24.2 -
# those are EOL-old; these versions are the closest modern equivalents. If HMM
# training throws version-related errors, pin down to upstream's exact versions
# in a separate venv/container for that engine.)
numpy>=1.24.0
scikit-learn>=1.3.0
rstr==3.0.0
